%h1 Your SIM cards

%table.table.table-condensed
  %tr
    %th 
    %th 
    %th Carrior
    %th Number
    %th Valid
    %th Registered at
    %th Imsi
    %th User
    %th
    %th
    %th

  - @sims.each do |sim|
    %tr
      %td= image_tag("sim_icon.png", height: 20)
      %td
        - if device = sim.attached_device
          = link_to(device.id, device_path(device))
        - else
          detached
      %td= sim.carrior
      %td= sim.number
      %td= sim.available
      %td= sim.registered_at
      %td= sim.imsi
      %td= sim.user_id
      %td= link_to 'Show', sim
      %td= link_to 'Edit', edit_sim_path(sim)
      %td= link_to 'Destroy', sim, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Sim', new_sim_path
